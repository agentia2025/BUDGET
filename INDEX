<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="theme-color" content="#0A0E1A">
<title>MonBudget — Gestionnaire intelligent</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
:root{
  --bg:#0A0E1A;--bg2:#111827;--bg3:#1C2333;
  --card:#161D2E;--card2:#1E2740;
  --border:rgba(255,255,255,.07);--border2:rgba(255,255,255,.12);
  --g1:#00E5A0;--g2:#00B87A;--g3:#007A52;
  --r1:#FF5C5C;--r2:#FF3B3B;
  --b1:#4F8EF7;--b2:#2D6BE4;
  --a1:#FFB547;--a2:#E09228;
  --p1:#A78BFA;
  --text:#F0F4FF;--text2:#8B95B0;--text3:#5A6480;
  --nav-h:70px;
}
html,body{height:100%;font-family:'DM Sans',sans-serif;background:var(--bg);color:var(--text);overflow:hidden}
.app{max-width:430px;margin:0 auto;height:100dvh;display:flex;flex-direction:column;position:relative;overflow:hidden}
.scroll{flex:1;overflow-y:auto;overflow-x:hidden;padding-bottom:calc(var(--nav-h) + 16px);scroll-behavior:smooth;-webkit-overflow-scrolling:touch}
.scroll::-webkit-scrollbar{display:none}
.view{display:none;animation:fadeUp .22s ease}.view.active{display:block}
@keyframes fadeUp{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}

/* ── BOTTOM NAV ── */
.bnav{position:absolute;bottom:0;left:0;right:0;height:var(--nav-h);background:rgba(10,14,26,.95);backdrop-filter:blur(24px);-webkit-backdrop-filter:blur(24px);border-top:0.5px solid var(--border2);display:flex;align-items:center;padding:0 8px env(safe-area-inset-bottom,8px);z-index:100}
.nb{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px;padding:8px 4px;border:none;background:transparent;cursor:pointer;color:var(--text3);font-size:10px;font-family:'DM Sans',sans-serif;font-weight:500;transition:color .2s;letter-spacing:.02em}
.nb svg{width:22px;height:22px;stroke:currentColor;fill:none;stroke-width:1.6;stroke-linecap:round;stroke-linejoin:round;transition:transform .2s}
.nb.active{color:var(--g1)}.nb.active svg{transform:scale(1.1)}
.nb-cam{flex:0 0 64px;display:flex;flex-direction:column;align-items:center;gap:4px;font-size:10px;color:var(--text3);border:none;background:transparent;cursor:pointer;font-family:'DM Sans',sans-serif;font-weight:500;margin-top:-20px;letter-spacing:.02em}
.cam-ring{width:54px;height:54px;border-radius:50%;background:linear-gradient(135deg,var(--g1),var(--g2));display:flex;align-items:center;justify-content:center;box-shadow:0 0 0 3px rgba(0,229,160,.18),0 8px 24px rgba(0,229,160,.32);transition:transform .15s,box-shadow .15s}
.cam-ring:active{transform:scale(.93)}
.cam-ring svg{width:24px;height:24px;stroke:#fff;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}

/* ── HERO ── */
.hero{margin:16px 16px 0;border-radius:24px;overflow:hidden;position:relative;padding:28px 24px 24px}
.hero-bg{position:absolute;inset:0;background:linear-gradient(135deg,#0D2E4A 0%,#0A1A2E 50%,#0D1F3C 100%)}
.hero-glow{position:absolute;top:-40px;right:-40px;width:200px;height:200px;border-radius:50%;background:radial-gradient(circle,rgba(0,229,160,.16) 0%,transparent 70%);pointer-events:none}
.hero-glow2{position:absolute;bottom:-60px;left:-30px;width:160px;height:160px;border-radius:50%;background:radial-gradient(circle,rgba(79,142,247,.09) 0%,transparent 70%);pointer-events:none}
.hero-label{font-size:11px;color:rgba(176,196,230,.6);letter-spacing:.09em;text-transform:uppercase;font-weight:600;margin-bottom:8px;position:relative;z-index:1}
.hero-val{font-size:40px;font-weight:700;letter-spacing:-.03em;line-height:1;position:relative;z-index:1;transition:color .3s}
.hero-val.pos{color:#fff}.hero-val.neg{color:var(--r1)}.hero-val.warn{color:var(--a1)}
.hero-sub{font-size:12px;color:rgba(176,196,230,.55);margin-top:10px;position:relative;z-index:1}
.hero-chips{display:flex;gap:8px;margin-top:16px;position:relative;z-index:1}
.chip{flex:1;background:rgba(255,255,255,.06);border:0.5px solid rgba(255,255,255,.09);border-radius:14px;padding:10px 8px;text-align:center}
.chip-l{font-size:9px;color:rgba(176,196,230,.5);margin-bottom:4px;text-transform:uppercase;letter-spacing:.07em;font-weight:600}
.chip-v{font-size:14px;font-weight:700;font-family:'DM Mono',monospace}
.chip-v.pos{color:var(--g1)}.chip-v.neg{color:var(--r1)}.chip-v.neu{color:var(--text)}

/* ── PROGRESS BAR ── */
.pbar-wrap{margin:12px 16px 0;border-radius:18px;background:var(--card);border:0.5px solid var(--border);padding:16px}
.pbar-head{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:10px}
.pbar-title{font-size:11px;color:var(--text2);font-weight:600;letter-spacing:.06em;text-transform:uppercase}
.pbar-pct{font-size:20px;font-weight:700;font-family:'DM Mono',monospace;color:var(--g1)}
.pbar-track{height:8px;background:rgba(255,255,255,.06);border-radius:4px;overflow:hidden;margin-bottom:8px}
.pbar-fill{height:100%;border-radius:4px;transition:width .5s cubic-bezier(.4,0,.2,1);background:linear-gradient(90deg,var(--g3),var(--g1))}
.pbar-fill.warn{background:linear-gradient(90deg,#C07800,var(--a1))}
.pbar-fill.bad{background:linear-gradient(90deg,#B02020,var(--r1))}
.pbar-ft{display:flex;justify-content:space-between;font-size:11px;color:var(--text3)}

/* ── SECTIONS ── */
.sec-title{font-size:11px;color:var(--text3);font-weight:700;letter-spacing:.1em;text-transform:uppercase;padding:18px 16px 10px}
.panel{margin:0 16px 12px;background:var(--card);border:0.5px solid var(--border);border-radius:20px;overflow:hidden}
.panel-inner{padding:16px}

/* ── CATÉGORIES ── */
.cat-row{display:flex;align-items:center;gap:12px;padding:11px 16px}
.cat-row:not(:last-child){border-bottom:0.5px solid var(--border)}
.cat-ico{font-size:18px;width:38px;height:38px;border-radius:12px;background:rgba(255,255,255,.05);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.cat-info{flex:1;min-width:0}
.cat-name{font-size:13px;font-weight:500;color:var(--text);margin-bottom:4px}
.cat-track{height:3px;background:rgba(255,255,255,.06);border-radius:2px;overflow:hidden}
.cat-fill{height:100%;border-radius:2px;background:linear-gradient(90deg,var(--g3),var(--g1))}
.cat-amt{font-size:13px;font-weight:600;color:var(--text);white-space:nowrap;font-family:'DM Mono',monospace}

/* ── TRANSACTIONS ── */
.tx-dh{padding:12px 16px 4px;font-size:11px;color:var(--text3);font-weight:700;text-transform:uppercase;letter-spacing:.08em}
.tx-row{display:flex;align-items:center;gap:12px;padding:12px 16px;transition:background .12s}
.tx-row:not(:last-child){border-bottom:0.5px solid var(--border)}
.tx-row:active{background:rgba(255,255,255,.03)}
.tx-ico2{width:40px;height:40px;border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0}
.tx-ico2.ic{background:rgba(0,229,160,.1);border:0.5px solid rgba(0,229,160,.18)}
.tx-ico2.fi{background:rgba(79,142,247,.1);border:0.5px solid rgba(79,142,247,.18)}
.tx-ico2.sc{background:rgba(167,139,250,.08);border:0.5px solid rgba(167,139,250,.18)}
.tx-ico2.ex{background:rgba(255,255,255,.04);border:0.5px solid var(--border)}
.tx-body{flex:1;min-width:0}
.tx-name2{font-size:14px;font-weight:500;color:var(--text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.tx-meta{display:flex;align-items:center;gap:6px;margin-top:2px;flex-wrap:wrap}
.tx-cat-lbl{font-size:11px;color:var(--text3)}
.badge2{font-size:9px;font-weight:700;padding:2px 7px;border-radius:20px;letter-spacing:.04em}
.badge2.scan{background:rgba(167,139,250,.14);color:var(--p1)}
.badge2.fix{background:rgba(79,142,247,.12);color:var(--b1)}
.badge2.rev{background:rgba(0,229,160,.12);color:var(--g1)}
.tx-right{display:flex;flex-direction:column;align-items:flex-end;gap:5px}
.tx-amt2{font-size:15px;font-weight:700;font-family:'DM Mono',monospace;white-space:nowrap}
.tx-amt2.pos{color:var(--g1)}.tx-amt2.neg{color:var(--text)}
.tx-del2{background:rgba(255,92,92,.1);border:none;border-radius:8px;color:var(--r1);cursor:pointer;font-size:10px;font-weight:600;padding:3px 8px;font-family:'DM Sans',sans-serif;letter-spacing:.02em;transition:background .12s}
.tx-del2:active{background:rgba(255,92,92,.2)}

/* ── FORMS ── */
.seg2{display:flex;background:rgba(255,255,255,.04);border:0.5px solid var(--border);border-radius:14px;overflow:hidden;margin-bottom:16px}
.seg2-btn{flex:1;padding:10px 6px;border:none;background:transparent;font-size:12px;font-weight:600;color:var(--text3);cursor:pointer;font-family:'DM Sans',sans-serif;transition:all .15s;letter-spacing:.03em}
.seg2-btn.on-income{background:rgba(0,229,160,.14);color:var(--g1)}
.seg2-btn.on-expense{background:rgba(255,92,92,.12);color:var(--r1)}
.seg2-btn.on-fixed{background:rgba(79,142,247,.12);color:var(--b1)}
.fg2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-bottom:14px}
.fi2{display:flex;flex-direction:column;gap:5px}
.fi2 label{font-size:10px;color:var(--text3);font-weight:700;text-transform:uppercase;letter-spacing:.09em}
.fi2 input,.fi2 select{padding:11px 12px;border:0.5px solid var(--border2);border-radius:12px;background:rgba(255,255,255,.04);color:var(--text);font-size:14px;font-family:'DM Sans',sans-serif;-webkit-appearance:none;appearance:none;outline:none;transition:border-color .15s,background .15s}
.fi2 input:focus,.fi2 select:focus{border-color:rgba(0,229,160,.45);background:rgba(0,229,160,.04)}
.fi2 select option{background:#1C2333;color:#F0F4FF}
.fi2.full{grid-column:1/-1}
.btn2{width:100%;padding:14px;border-radius:14px;border:none;font-size:15px;font-weight:700;font-family:'DM Sans',sans-serif;cursor:pointer;transition:all .15s;letter-spacing:.02em}
.btn2.g{background:linear-gradient(135deg,var(--g2),var(--g1));color:#052E1A;box-shadow:0 4px 20px rgba(0,229,160,.22)}
.btn2.g:active{transform:scale(.98);box-shadow:0 2px 10px rgba(0,229,160,.12)}
.btn2.r{background:rgba(255,92,92,.14);color:var(--r1);border:0.5px solid rgba(255,92,92,.2)}
.btn2.r:active{background:rgba(255,92,92,.22)}
.btn2.ghost{background:rgba(255,255,255,.05);color:var(--text2);border:0.5px solid var(--border2)}
.btn2.ghost:active{background:rgba(255,255,255,.08)}

/* ── SCANNER ── */
.uzone2{border:1.5px dashed rgba(255,255,255,.14);border-radius:20px;padding:30px 16px;text-align:center;cursor:pointer;transition:all .2s;position:relative;overflow:hidden;background:rgba(255,255,255,.015)}
.uzone2:hover,.uzone2:focus-within{border-color:rgba(0,229,160,.38);background:rgba(0,229,160,.04)}
.uzone2 input{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%}
.uzone2 svg{width:34px;height:34px;stroke:rgba(0,229,160,.65);margin:0 auto 10px;display:block}
.prev2{width:100%;max-height:180px;object-fit:contain;border-radius:16px;border:0.5px solid var(--border2);margin:14px 0;display:none}
.spin2{display:inline-block;width:14px;height:14px;border:2px solid rgba(0,229,160,.15);border-top-color:var(--g1);border-radius:50%;animation:sp .7s linear infinite;vertical-align:middle;margin-right:6px}
@keyframes sp{to{transform:rotate(360deg)}}
.scan-box2{border:1.5px solid rgba(0,229,160,.22);border-radius:20px;padding:16px;margin-top:12px;display:none;background:rgba(0,229,160,.025)}
.cr2{display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:0.5px solid var(--border)}
.cr2:last-child{border-bottom:none}
.cr2-lbl{font-size:10px;color:var(--text3);font-weight:700;text-transform:uppercase;letter-spacing:.07em;width:72px;flex-shrink:0}
.cr2 input,.cr2 select{flex:1;padding:7px 10px;border:0.5px solid var(--border2);border-radius:10px;background:rgba(255,255,255,.04);color:var(--text);font-size:13px;font-family:'DM Sans',sans-serif;outline:none;-webkit-appearance:none;appearance:none}
.cr2 select{text-align:left}
.cr2 select option{background:#1C2333;color:#F0F4FF}
.cr2-items{flex:1;font-size:11px;color:var(--text3);text-align:right;line-height:1.5}
.scan-btns{display:flex;gap:8px;margin-top:14px}
.manual-sep{display:flex;align-items:center;gap:12px;margin:18px 16px}
.manual-sep::before,.manual-sep::after{content:'';flex:1;height:0.5px;background:var(--border)}
.manual-sep span{font-size:11px;color:var(--text3);white-space:nowrap;font-weight:600;letter-spacing:.05em}
.manual-input{padding:11px 12px;border:0.5px solid var(--border2);border-radius:12px;background:rgba(255,255,255,.04);color:var(--text);font-size:14px;font-family:'DM Sans',sans-serif;-webkit-appearance:none;appearance:none;outline:none;width:100%}
.manual-input:focus{border-color:rgba(0,229,160,.45)}

/* ── HISTORIQUE ── */
.hf-row{display:flex;gap:7px;padding:0 16px 14px;overflow-x:auto;scrollbar-width:none}
.hf-row::-webkit-scrollbar{display:none}
.hf-btn{flex-shrink:0;padding:7px 15px;border-radius:20px;border:0.5px solid var(--border2);background:transparent;color:var(--text3);font-size:12px;font-weight:600;cursor:pointer;font-family:'DM Sans',sans-serif;transition:all .15s;white-space:nowrap;letter-spacing:.02em}
.hf-btn.on{background:var(--g1);color:#052E1A;border-color:var(--g1)}
.hist-mh{display:flex;justify-content:space-between;align-items:baseline;padding:14px 16px 6px}
.hist-mh-l{font-size:11px;color:var(--text3);font-weight:700;text-transform:uppercase;letter-spacing:.08em}
.hist-mh-v{font-size:14px;font-weight:700;font-family:'DM Mono',monospace}

/* ── MESSAGES ── */
.msg2{padding:11px 14px;border-radius:12px;font-size:13px;margin:10px 0;font-weight:500}
.msg2.ok{background:rgba(0,229,160,.1);color:var(--g1);border:0.5px solid rgba(0,229,160,.2)}
.msg2.err{background:rgba(255,92,92,.1);color:var(--r1);border:0.5px solid rgba(255,92,92,.2)}
.empty2{text-align:center;padding:28px 16px;color:var(--text3);font-size:14px}
</style>
</head>
<body>
<div class="app">
<div class="scroll" id="scroll-area">

<!-- ══ DASHBOARD ══ -->
<div id="v-dash" class="view active">
  <div class="hero">
    <div class="hero-bg"></div><div class="hero-glow"></div><div class="hero-glow2"></div>
    <div class="hero-label">Solde disponible ce mois</div>
    <div class="hero-val pos" id="h-val">0,00 €</div>
    <div class="hero-sub" id="h-sub">Chargement…</div>
    <div class="hero-chips">
      <div class="chip"><div class="chip-l">Revenus</div><div class="chip-v pos" id="k-rev">0 €</div></div>
      <div class="chip"><div class="chip-l">Charges</div><div class="chip-v neg" id="k-fix">0 €</div></div>
      <div class="chip"><div class="chip-l">Dépenses</div><div class="chip-v neu" id="k-exp">0 €</div></div>
    </div>
  </div>

  <div class="pbar-wrap">
    <div class="pbar-head"><span class="pbar-title">Budget utilisé</span><span class="pbar-pct" id="pbar-pct">0%</span></div>
    <div class="pbar-track"><div class="pbar-fill" id="pbar" style="width:0%"></div></div>
    <div class="pbar-ft"><span id="pbar-l">0 € utilisé</span><span id="pbar-r">0 € restant</span></div>
  </div>

  <div class="sec-title">Répartition des dépenses</div>
  <div class="panel" id="cat-bd"><div class="empty2">Aucune dépense ce mois</div></div>

  <div class="sec-title">Transactions récentes</div>
  <div class="panel" id="dash-recent"><div class="empty2">Aucune transaction</div></div>
</div>

<!-- ══ SAISIR ══ -->
<div id="v-tx" class="view">
  <div class="sec-title">Nouvelle transaction</div>
  <div class="panel"><div class="panel-inner">
    <div class="seg2">
      <button class="seg2-btn on-income" id="sb-inc" onclick="setSeg('income')">+ Revenu</button>
      <button class="seg2-btn" id="sb-exp" onclick="setSeg('expense')">− Dépense</button>
      <button class="seg2-btn" id="sb-fix" onclick="setSeg('fixed')">⊟ Fixe</button>
    </div>
    <div class="fg2">
      <div class="fi2 full"><label>Libellé</label><input type="text" id="tx-lbl" placeholder="Salaire, courses, loyer…"></div>
      <div class="fi2"><label>Montant €</label><input type="number" id="tx-amt" placeholder="0.00" min="0" step="0.01" inputmode="decimal"></div>
      <div class="fi2"><label>Date</label><input type="date" id="tx-date"></div>
      <div class="fi2 full"><label>Catégorie</label>
        <select id="tx-cat">
          <optgroup label="── Revenus"><option>Salaire</option><option>Prime / Bonus</option><option>Remboursement</option><option>Autre revenu</option></optgroup>
          <optgroup label="── Charges fixes"><option>Loyer</option><option>Énergie / EDF</option><option>Assurance</option><option>Abonnement</option><option>Crédit</option></optgroup>
          <optgroup label="── Dépenses"><option>Alimentation</option><option>Restaurant</option><option>Transport</option><option>Santé</option><option>Loisirs</option><option>Vêtements</option><option>Maison</option><option>Autre</option></optgroup>
        </select>
      </div>
    </div>
    <div id="tx-fb"></div>
    <button class="btn2 g" onclick="addTx()">Ajouter</button>
  </div></div>

  <div class="sec-title">Ce mois-ci</div>
  <div class="panel" id="txlist"><div class="empty2">Aucune transaction ce mois</div></div>
</div>

<!-- ══ SCANNER ══ -->
<div id="v-scan" class="view">
  <div class="sec-title">Scanner un ticket</div>
  <div class="panel"><div class="panel-inner">
    <div class="uzone2">
      <input type="file" accept="image/*" capture="environment" id="sf" onchange="handleScan(event)">
      <svg viewBox="0 0 24 24"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
      <div style="font-size:16px;font-weight:600;color:var(--text);margin-bottom:4px">Photographier ou importer</div>
      <div style="font-size:12px;color:var(--text3)">Ticket · Reçu · Facture</div>
    </div>
    <img id="sprev" class="prev2">
    <div id="sst"></div>
  </div></div>

  <div class="scan-box2" id="sc-box">
    <div style="font-size:11px;font-weight:700;color:var(--g1);text-transform:uppercase;letter-spacing:.09em;margin-bottom:12px">✓ Vérifiez et confirmez</div>
    <div class="cr2"><span class="cr2-lbl">Commerce</span><input type="text" id="sc-merch" placeholder="Nom du commerce"></div>
    <div class="cr2"><span class="cr2-lbl">Montant</span><input type="number" id="sc-amt" step="0.01" min="0" placeholder="0.00" inputmode="decimal"></div>
    <div class="cr2"><span class="cr2-lbl">Date</span><input type="date" id="sc-date"></div>
    <div class="cr2"><span class="cr2-lbl">Catégorie</span>
      <select id="sc-cat"><option>Alimentation</option><option>Restaurant</option><option>Transport</option><option>Santé</option><option>Loisirs</option><option>Vêtements</option><option>Maison</option><option>Énergie / EDF</option><option>Abonnement</option><option>Autre</option></select>
    </div>
    <div class="cr2" id="sc-ir" style="display:none"><span class="cr2-lbl">Articles</span><span class="cr2-items" id="sc-items"></span></div>
    <div class="scan-btns">
      <button class="btn2 g" onclick="confirmScan()" style="flex:2">✓ Enregistrer</button>
      <button class="btn2 ghost" onclick="resetScan()" style="flex:1">Annuler</button>
    </div>
  </div>

  <div class="manual-sep"><span>Ou saisie manuelle rapide</span></div>
  <div class="panel"><div class="panel-inner">
    <div class="fg2">
      <div class="fi2 full"><input class="manual-input" type="text" id="m-lbl" placeholder="Nom de la dépense"></div>
      <div class="fi2"><label>Montant</label><input type="number" id="m-amt" placeholder="0.00" step="0.01" min="0" inputmode="decimal"></div>
      <div class="fi2"><label>Date</label><input type="date" id="m-date"></div>
      <div class="fi2 full"><label>Catégorie</label>
        <select id="m-cat"><option>Alimentation</option><option>Restaurant</option><option>Transport</option><option>Santé</option><option>Loisirs</option><option>Vêtements</option><option>Maison</option><option>Abonnement</option><option>Autre</option></select>
      </div>
    </div>
    <button class="btn2 r" onclick="addManual()">− Ajouter dépense</button>
  </div></div>
</div>

<!-- ══ HISTORIQUE ══ -->
<div id="v-hist" class="view">
  <div class="sec-title">Historique complet</div>
  <div class="hf-row">
    <button class="hf-btn on" id="hf-all" onclick="setHF('all',this)">Tout</button>
    <button class="hf-btn" onclick="setHF('income',this)">+ Revenus</button>
    <button class="hf-btn" onclick="setHF('fixed',this)">Charges fixes</button>
    <button class="hf-btn" onclick="setHF('expense',this)">Dépenses</button>
    <button class="hf-btn" onclick="setHF('scan',this)">📷 Scannés</button>
  </div>
  <div id="hist-list"></div>
</div>

</div><!-- /scroll -->

<!-- ══ BOTTOM NAV ══ -->
<nav class="bnav">
  <button class="nb active" id="nb-dash" onclick="go('dash')">
    <svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/></svg>
    Tableau
  </button>
  <button class="nb" id="nb-tx" onclick="go('tx')">
    <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
    Saisir
  </button>
  <button class="nb-cam" onclick="go('scan')">
    <div class="cam-ring">
      <svg viewBox="0 0 24 24"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
    </div>
    Scanner
  </button>
  <button class="nb" id="nb-hist" onclick="go('hist')">
    <svg viewBox="0 0 24 24"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
    Historique
  </button>
</nav>
</div>

<script>
const SK='monbudget_v4';
let S={transactions:[]};
function load(){try{const d=localStorage.getItem(SK);if(d)S=JSON.parse(d)}catch(e){}}
function save(){try{localStorage.setItem(SK,JSON.stringify(S))}catch(e){}}

const icons={Salaire:'💰','Prime / Bonus':'🎁',Remboursement:'↩️','Autre revenu':'💳',Loyer:'🏠','Énergie / EDF':'⚡',Assurance:'🛡️',Abonnement:'📱',Crédit:'🏦',Alimentation:'🛒',Restaurant:'🍽️',Transport:'🚗',Santé:'💊',Loisirs:'🎮',Vêtements:'👔',Maison:'🏡',Autre:'📌'};

function fmt(n){
  if(n===0||Object.is(n,-0))return'0,00 €';
  const a=new Intl.NumberFormat('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Math.abs(n));
  return(n<0?'−':'')+a+' €';
}
function fmtS(n){
  if(n===0)return'0,00 €';
  const a=new Intl.NumberFormat('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Math.abs(n));
  return(n>=0?'+':'−')+a+' €';
}
function today(){return new Date().toISOString().split('T')[0]}
function safeDate(d){const t=new Date(d+'T12:00');return isNaN(t.getTime())?today():d}
function weeksLeft(){const n=new Date();const e=new Date(n.getFullYear(),n.getMonth()+1,0);return Math.max(1,Math.ceil((e-n)/(7*86400000)))}
function monthTxs(){const n=new Date();return S.transactions.filter(t=>{const d=new Date(t.date);return d.getMonth()===n.getMonth()&&d.getFullYear()===n.getFullYear()})}
function extractJSON(text){
  try{const m=text.match(/\{[\s\S]*\}/);if(!m)return null;
    const o=JSON.parse(m[0]);
    if(typeof o.amount==='string')o.amount=parseFloat(o.amount.replace(',','.'))||0;
    else o.amount=parseFloat(o.amount)||0;
    return o;
  }catch(e){return null}
}

let activeView='dash';
function go(v){
  document.querySelectorAll('.view').forEach(el=>el.classList.remove('active'));
  document.querySelectorAll('.nb').forEach(el=>el.classList.remove('active'));
  document.getElementById('v-'+v).classList.add('active');
  const nb=document.getElementById('nb-'+v);if(nb)nb.classList.add('active');
  document.getElementById('scroll-area').scrollTop=0;
  activeView=v;
  if(v==='dash')renderDash();
  if(v==='tx'){initTxForm();renderTxList()}
  if(v==='hist')renderHist();
  if(v==='scan')document.getElementById('m-date').value=today();
}

let seg='income';
function setSeg(s){
  seg=s;
  ['inc','exp','fix'].forEach(x=>{document.getElementById('sb-'+x).className='seg2-btn'});
  const map={income:'inc',expense:'exp',fixed:'fix'};
  document.getElementById('sb-'+map[s]).className='seg2-btn on-'+s;
  const cat=document.getElementById('tx-cat'),lbl=document.getElementById('tx-lbl');
  if(s==='income'){lbl.placeholder='Salaire, prime, remboursement…';cat.value='Salaire'}
  else if(s==='fixed'){lbl.placeholder='Loyer, EDF, assurance, crédit…';cat.value='Loyer'}
  else{lbl.placeholder='Courses, restaurant, transport…';cat.value='Alimentation'}
}
function initTxForm(){document.getElementById('tx-date').value=today();setSeg('income');document.getElementById('tx-lbl').value='';document.getElementById('tx-amt').value=''}

function addTx(){
  const label=document.getElementById('tx-lbl').value.trim();
  const amount=parseFloat(document.getElementById('tx-amt').value);
  if(!label||!amount||amount<=0)return;
  S.transactions.unshift({id:Date.now(),label,amount:amount*(seg==='income'?1:-1),date:safeDate(document.getElementById('tx-date').value||today()),cat:document.getElementById('tx-cat').value,type:seg,source:'manual'});
  save();document.getElementById('tx-lbl').value='';document.getElementById('tx-amt').value='';
  renderTxList();renderDash();flash('tx-fb','Transaction ajoutée !','ok');
}

function renderTxList(){
  const txs=monthTxs();const wrap=document.getElementById('txlist');
  if(!txs.length){wrap.innerHTML='<div class="empty2">Aucune transaction ce mois</div>';return}
  const byD={};txs.forEach(t=>{if(!byD[t.date])byD[t.date]=[];byD[t.date].push(t)});
  const dates=Object.keys(byD).sort((a,b)=>b.localeCompare(a));
  wrap.innerHTML=dates.map(d=>{
    const dl=new Date(d+'T12:00').toLocaleDateString('fr-FR',{weekday:'short',day:'numeric',month:'short'});
    return`<div class="tx-dh">${dl}</div>${byD[d].map(txRow).join('')}`;
  }).join('');
}

function txRow(t){
  const pos=t.amount>=0;
  const ico=icons[t.cat]||'📌';
  const icoCls=t.type==='income'?'ic':t.type==='fixed'?'fi':t.source==='scan'?'sc':'ex';
  const badge=t.source==='scan'?'<span class="badge2 scan">SCAN</span>':t.type==='fixed'?'<span class="badge2 fix">FIXE</span>':t.type==='income'?'<span class="badge2 rev">REVENU</span>':'';
  return`<div class="tx-row">
    <div class="tx-ico2 ${icoCls}">${ico}</div>
    <div class="tx-body"><div class="tx-name2">${t.label}</div><div class="tx-meta"><span class="tx-cat-lbl">${t.cat}</span>${badge}</div></div>
    <div class="tx-right">
      <span class="tx-amt2 ${pos?'pos':'neg'}">${pos?'+':'−'}${fmt(Math.abs(t.amount))}</span>
      <button class="tx-del2" onclick="delTx(${t.id})">✕ Suppr.</button>
    </div>
  </div>`;
}

function delTx(id){
  S.transactions=S.transactions.filter(t=>t.id!==id);save();renderDash();
  if(activeView==='tx')renderTxList();
  if(activeView==='hist')renderHist();
}

function renderDash(){
  const txs=monthTxs();
  const rev=txs.filter(t=>t.type==='income').reduce((s,t)=>s+t.amount,0);
  const fix=txs.filter(t=>t.type==='fixed').reduce((s,t)=>s+Math.abs(t.amount),0);
  const exp=txs.filter(t=>t.type==='expense').reduce((s,t)=>s+Math.abs(t.amount),0);
  const solde=rev-fix-exp;const wl=weeksLeft();const weekly=solde/wl;
  const used=rev>0?Math.min(100,Math.round((fix+exp)/rev*100)):0;

  const hv=document.getElementById('h-val');
  hv.textContent=fmt(solde);hv.className='hero-val '+(solde<0?'neg':solde<200?'warn':'pos');
  document.getElementById('h-sub').textContent=fmt(Math.abs(weekly))+'/semaine · '+wl+' sem. restante'+(wl>1?'s':'');
  document.getElementById('k-rev').textContent='+'+fmt(rev);
  document.getElementById('k-fix').textContent=fix>0?'−'+fmt(fix):'0,00 €';
  document.getElementById('k-exp').textContent=exp>0?'−'+fmt(exp):'0,00 €';

  const pb=document.getElementById('pbar');pb.style.width=used+'%';
  pb.className='pbar-fill'+(used>90?' bad':used>70?' warn':'');
  const pe=document.getElementById('pbar-pct');pe.textContent=used+'%';
  pe.style.color=used>90?'var(--r1)':used>70?'var(--a1)':'var(--g1)';
  document.getElementById('pbar-l').textContent=fmt(fix+exp)+' utilisé';
  document.getElementById('pbar-r').textContent=fmt(Math.max(0,solde))+' restant';

  const expTxs=txs.filter(t=>t.type==='expense');
  const byCat={};expTxs.forEach(t=>{if(!byCat[t.cat])byCat[t.cat]=0;byCat[t.cat]+=Math.abs(t.amount)});
  const entries=Object.entries(byCat).sort((a,b)=>b[1]-a[1]);
  const cbd=document.getElementById('cat-bd');
  if(!entries.length){cbd.innerHTML='<div class="empty2">Aucune dépense ce mois</div>'}
  else{
    const tot=entries.reduce((s,e)=>s+e[1],0);
    cbd.innerHTML=entries.map(([cat,amt])=>{
      const p=tot>0?Math.round(amt/tot*100):0;
      return`<div class="cat-row"><div class="cat-ico">${icons[cat]||'📌'}</div>
        <div class="cat-info"><div class="cat-name">${cat}</div><div class="cat-track"><div class="cat-fill" style="width:${p}%"></div></div></div>
        <span class="cat-amt">${fmt(amt)}</span></div>`;
    }).join('');
  }
  const recent=txs.slice(0,5);
  const dr=document.getElementById('dash-recent');
  dr.innerHTML=recent.length?recent.map(txRow).join(''):'<div class="empty2">Aucune transaction</div>';
}

let hf='all';
function setHF(f,btn){
  hf=f;document.querySelectorAll('.hf-btn').forEach(b=>b.classList.remove('on'));btn.classList.add('on');renderHist();
}
function renderHist(){
  let txs=[...S.transactions].sort((a,b)=>b.date.localeCompare(a.date));
  if(hf==='scan')txs=txs.filter(t=>t.source==='scan');
  else if(hf!=='all')txs=txs.filter(t=>t.type===hf);
  const list=document.getElementById('hist-list');
  if(!txs.length){list.innerHTML='<div class="empty2">Aucune transaction</div>';return}
  const byM={};txs.forEach(t=>{const k=t.date.substring(0,7);if(!byM[k])byM[k]=[];byM[k].push(t)});
  const months=Object.keys(byM).sort((a,b)=>b.localeCompare(a));
  list.innerHTML=months.map(m=>{
    const[y,mo]=m.split('-');
    const ml=new Date(parseInt(y),parseInt(mo)-1,1).toLocaleDateString('fr-FR',{month:'long',year:'numeric'});
    const items=byM[m];const sum=items.reduce((s,t)=>s+t.amount,0);
    return`<div class="hist-mh"><span class="hist-mh-l">${ml}</span><span class="hist-mh-v" style="color:${sum>=0?'var(--g1)':'var(--r1)'}">${fmtS(sum)}</span></div>
    <div class="panel" style="margin:0 16px 12px">${items.map(txRow).join('')}</div>`;
  }).join('');
}

let sB64=null,sMime=null;
function handleScan(e){
  const file=e.target.files[0];if(!file)return;
  const reader=new FileReader();
  reader.onload=ev=>{
    sB64=ev.target.result.split(',')[1];sMime=file.type;
    const p=document.getElementById('sprev');p.src=ev.target.result;p.style.display='block';
    document.getElementById('sc-box').style.display='none';analyzeReceipt();
  };reader.readAsDataURL(file);
}
function setSt(html){document.getElementById('sst').innerHTML=html}
async function analyzeReceipt(){
  if(!sB64)return;
  setSt('<div class="msg2 ok"><span class="spin2"></span>Analyse IA en cours…</div>');
  const prompt=`Analyse ce ticket de caisse ou facture. Réponds UNIQUEMENT avec un objet JSON valide, sans markdown ni texte autour :
{"merchant":"nom","amount":12.50,"date":"${today()}","category":"Alimentation","items":"article1, article2"}
Catégories: Alimentation, Restaurant, Transport, Santé, Loisirs, Vêtements, Maison, Énergie / EDF, Abonnement, Autre.`;
  try{
    const resp=await fetch('https://api.anthropic.com/v1/messages',{
      method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:400,messages:[{role:'user',content:[
        {type:'image',source:{type:'base64',media_type:sMime||'image/jpeg',data:sB64}},
        {type:'text',text:prompt}
      ]}]})
    });
    if(!resp.ok)throw new Error('HTTP '+resp.status);
    const data=await resp.json();
    const raw=(data.content||[]).map(b=>b.text||'').join('');
    const parsed=extractJSON(raw);
    if(parsed){populateScan(parsed);setSt('')}
    else setSt('<div class="msg2 err">Lecture impossible — saisie manuelle ci-dessous</div>');
  }catch(err){setSt('<div class="msg2 err">'+err.message+'</div>')}
}
function populateScan(d){
  document.getElementById('sc-merch').value=d.merchant||'';
  document.getElementById('sc-amt').value=d.amount||'';
  document.getElementById('sc-date').value=d.date||today();
  const s=document.getElementById('sc-cat');for(let o of s.options)if(o.value===d.category){s.value=d.category;break}
  const ir=document.getElementById('sc-ir');
  if(d.items&&d.items.trim()){document.getElementById('sc-items').textContent=d.items;ir.style.display='flex'}else ir.style.display='none';
  document.getElementById('sc-box').style.display='block';
}
function confirmScan(){
  const merchant=document.getElementById('sc-merch').value.trim();
  const amount=parseFloat(document.getElementById('sc-amt').value);
  if(!merchant||!amount||amount<=0){setSt('<div class="msg2 err">Commerce et montant requis</div>');return}
  S.transactions.unshift({id:Date.now(),label:merchant,amount:-Math.abs(amount),date:safeDate(document.getElementById('sc-date').value||today()),cat:document.getElementById('sc-cat').value,type:'expense',source:'scan',items:document.getElementById('sc-items').textContent});
  save();renderDash();setSt('<div class="msg2 ok">✓ Enregistré !</div>');
  setTimeout(()=>{resetScan();go('hist')},900);
}
function addManual(){
  const label=document.getElementById('m-lbl').value.trim();
  const amount=parseFloat(document.getElementById('m-amt').value);
  if(!label||!amount||amount<=0)return;
  S.transactions.unshift({id:Date.now(),label,amount:-Math.abs(amount),date:safeDate(document.getElementById('m-date').value||today()),cat:document.getElementById('m-cat').value,type:'expense',source:'manual'});
  save();renderDash();
  document.getElementById('m-lbl').value='';document.getElementById('m-amt').value='';
  setSt('<div class="msg2 ok">✓ Dépense ajoutée !</div>');
  setTimeout(()=>{go('hist')},800);
}
function resetScan(){
  sB64=null;sMime=null;
  const p=document.getElementById('sprev');p.style.display='none';p.src='';
  document.getElementById('sc-box').style.display='none';
  document.getElementById('sst').innerHTML='';document.getElementById('sf').value='';
}
function flash(id,msg,type){
  const el=document.getElementById(id);
  el.innerHTML=`<div class="msg2 ${type}" style="margin-bottom:8px">${msg}</div>`;
  setTimeout(()=>el.innerHTML='',2000);
}

// INIT
load();
document.getElementById('tx-date').value=today();
document.getElementById('m-date').value=today();
renderDash();
</script>
</body>
</html>
